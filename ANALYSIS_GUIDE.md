# 테스트 결과 분석 및 보고서 생성 가이드

## 🎯 개요

테스트 실행 후 결과를 통계적으로 분석하고 시각화된 보고서를 자동 생성하는 종합 가이드입니다.

---

## 📊 분석 파이프라인

```
1. 테스트 실행 (run_test_case.py)
   └─> data/test_results/*.json

2. 통계 분석 (scripts/analyze_results.py)
   └─> data/analysis/summary_report.json

3. 차트 생성 (scripts/generate_charts.py)
   └─> data/charts/*.png (9개 차트)

4. HTML 보고서 생성 (scripts/generate_report.py)
   └─> data/reports/final_report.html
```

---

## 🚀 빠른 시작

### 1단계: 테스트 실행

```bash
# Phase 1 전체 실행 (플랫폼 비교)
python run_test_case.py --tc TC-001 --products 3
python run_test_case.py --tc TC-002 --products 3
python run_test_case.py --tc TC-003 --products 3

# 또는 Phase별 일괄 실행 (추후 구현)
python run_test_phase.py --phase 1
```

**결과 저장 위치:**
```
data/test_results/
├── TC-001_20250101_120000.json
├── TC-002_20250101_130000.json
└── TC-003_20250101_140000.json
```

### 2단계: 통계 분석

```bash
# 모든 결과 분석
python scripts/analyze_results.py

# 특정 테스트 케이스만 분석
python scripts/analyze_results.py --tc-pattern TC-001

# 출력 파일 지정
python scripts/analyze_results.py --output data/analysis/phase1_report.json
```

**분석 내용:**
- 기본 통계 (평균, 표준편차, 중앙값)
- 개선율 계산
- t-test (플랫폼 비교)
- ANOVA (경로 비교)
- Pearson 상관관계 (체류시간 vs 순위)
- Cohen's d (효과 크기)

### 3단계: 차트 생성

```bash
# 9개 차트 자동 생성
python scripts/generate_charts.py

# 보고서 및 출력 디렉토리 지정
python scripts/generate_charts.py \
  --report data/analysis/summary_report.json \
  --output-dir data/charts
```

**생성되는 차트:**
1. `1_platform_rank_change.png` - 플랫폼별 평균 순위 상승폭
2. `2_platform_success_rate.png` - 플랫폼별 순위 개선 성공률
3. `3_path_comparison.png` - 진입 경로별 순위 상승 효과
4. `4_behavior_patterns.png` - 행동 패턴별 효과 비교
5. `5_dwell_time_correlation.png` - 체류 시간 vs 순위 변화 상관관계
6. `6_scale_effect.png` - 트래픽 양에 따른 순위 변화
7. `7_roi_comparison.png` - 트래픽 양별 ROI 비교
8. `8_category_comparison.png` - 카테고리별 효과성 비교
9. `9_overall_summary_heatmap.png` - 전체 테스트 요약 히트맵

### 4단계: HTML 보고서 생성

```bash
# 종합 보고서 생성
python scripts/generate_report.py

# 파일 경로 지정
python scripts/generate_report.py \
  --report data/analysis/summary_report.json \
  --charts-dir data/charts \
  --output data/reports/final_report.html
```

**보고서 구조:**
- Executive Summary (주요 발견 사항)
- Phase 1: 플랫폼별 효과 비교
- Phase 2: 진입 경로별 효과 비교
- Phase 3: 행동 패턴 최적화
- Phase 4: 트래픽 스케일 효과
- Phase 5: 카테고리별 검증
- 결론 및 최종 권장사항

**보고서 확인:**
```bash
# Windows
start data/reports/final_report.html

# Mac
open data/reports/final_report.html

# Linux
xdg-open data/reports/final_report.html
```

---

## 📈 분석 지표 설명

### 1. 기본 통계

#### 평균 순위 변화 (Mean Rank Change)
```python
mean_rank_change = np.mean(rank_changes)
```
- **의미**: 평균적으로 몇 위 상승/하락했는지
- **음수**: 순위 상승 (좋음)
- **양수**: 순위 하락 (나쁨)
- **예시**: -3.8위 → 평균 3.8위 상승

#### 표준편차 (Standard Deviation)
```python
std_rank_change = np.std(rank_changes)
```
- **의미**: 순위 변동의 일관성
- **낮을수록**: 일관된 효과 (안정적)
- **높을수록**: 변동 심함 (불안정)
- **예시**: ±2.1 → 평균적으로 2.1위 범위 내 변동

#### 개선율 (Improvement Rate)
```python
improvement_rate = (순위 상승 건수 / 전체 건수) * 100
```
- **의미**: 순위가 상승한 비율
- **범위**: 0~100%
- **예시**: 73% → 100회 중 73회 순위 상승

### 2. 추론 통계

#### t-test (독립 표본 t-검정)
```python
t_stat, p_value = ttest_ind(mobile_results, pc_results)
```
- **목적**: 두 그룹 간 평균 차이가 통계적으로 유의미한지 검증
- **사용 예**: 모바일 vs PC 효과 비교
- **p-value < 0.05**: 통계적으로 유의미한 차이 존재 (95% 신뢰도)
- **해석**:
  - p = 0.003 → 모바일과 PC의 효과 차이가 우연이 아님 (신뢰 가능)
  - p = 0.421 → 차이가 우연일 가능성 높음 (신뢰 불가)

#### ANOVA (분산 분석)
```python
f_stat, p_value = f_oneway(search, blog, cafe, shopping)
```
- **목적**: 3개 이상 그룹 간 평균 차이 검증
- **사용 예**: 통합검색 vs 블로그 vs 카페 vs 쇼핑검색 비교
- **p-value < 0.05**: 그룹 간 유의미한 차이 존재
- **사후 분석**: 어느 그룹이 우수한지 개별 비교 필요

#### Pearson 상관관계
```python
correlation, p_value = pearsonr(dwell_times, rank_changes)
```
- **목적**: 두 변수 간 선형 관계 측정
- **사용 예**: 체류 시간 vs 순위 상승 관계
- **상관계수 (r)**:
  - r > 0.6: 강한 양의 상관관계
  - 0.3 < r < 0.6: 중간 상관관계
  - r < 0.3: 약한 상관관계
  - r < 0: 음의 상관관계
- **예시**: r = -0.72 → 체류 시간이 길수록 순위 상승폭 큼

#### Cohen's d (효과 크기)
```python
effect_size = abs(mean_rank_change / std_rank_change)
```
- **목적**: 효과의 실질적 크기 측정
- **해석**:
  - d < 0.2: 작은 효과
  - 0.2 ≤ d < 0.5: 중간 효과
  - 0.5 ≤ d < 0.8: 큰 효과
  - d ≥ 0.8: 매우 큰 효과
- **예시**: d = 1.81 → 매우 큰 효과 (평균 변화가 표준편차의 1.81배)

### 3. ROI 지표

#### ROI (투자 대비 수익률)
```python
roi = avg_rank_change / total_time_minutes
```
- **의미**: 시간당 순위 상승폭
- **사용**: 비용 효율성 평가
- **예시**:
  - 10회: ROI = 0.0533 (1.6위 / 30분)
  - 50회: ROI = 0.0373 (4.7위 / 125분)
  - 100회: ROI = 0.0320 (8.0위 / 250분)
- **해석**: 10회가 시간 대비 가장 효율적

---

## 📊 차트 해석 가이드

### 1. 플랫폼별 평균 순위 상승폭 (막대 그래프)

```
     순위 변화
      ↑
    0 ├─────────────────
      │  ████
   -2 │  ████
      │  ████         ██
   -4 │  ████         ██        ████
      │  ████         ██        ████
      └──────────────────────────────→
        모바일      PC       혼합
```

**해석:**
- 막대가 아래로 길수록 순위 상승폭 큼 (좋음)
- 모바일이 PC보다 효과적
- 혼합은 중간 효과

### 2. 플랫폼별 성공률 (가로 막대)

```
모바일 100%  ████████████████ 75%
PC 100%      ██████████ 50%
혼합 70:30   ██████████████ 68%
            ─────────────────────────→
            0%   25%   50%   75%  100%
```

**해석:**
- 모바일이 가장 높은 성공률 (75%)
- PC는 상대적으로 낮음 (50%)
- 혼합은 균형 잡힌 성공률

### 3. 진입 경로별 효과 (산점도)

```
     순위 변화
      ↑
    0 ├───────●─────────  (카페: -1.5위)
      │
   -2 │     ●             (쇼핑: -2.0위)
      │
   -4 │  ●                (통합: -3.5위)
      │
   -6 │       ●           (블로그: -5.2위)
      └──────────────────────────────→
        통합  블로그  쇼핑  카페
```

**해석:**
- 블로그가 가장 효과적 (-5.2위)
- 통합검색도 우수 (-3.5위)
- 카페는 상대적으로 약함 (-1.5위)

### 4. 체류 시간 vs 순위 변화 상관관계

```
     순위 변화
      ↑
    0 │
      │  ●
   -2 │    ●
      │      ●
   -4 │        ●──── 추세선 (r=-0.85)
      │          ●
   -6 │
      └──────────────────────────────→
        0    50   100  150  200  250
                 체류 시간 (초)
```

**해석:**
- r = -0.85: 강한 음의 상관관계
- 체류 시간이 길수록 순위 상승폭 큼
- 추세선이 아래로 향함 (순위 상승)

### 5. 트래픽 양에 따른 순위 변화 (라인)

```
     누적 순위 변화
      ↑
    0 │●
      │  ●
   -5 │    ●●
      │       ●●
  -10 │          ●●●
      │              ●●●●
  -15 │                   ●●●●
      │                       ●●●
  -20 │                          ●●
      └────────────────────────────→
        0   10   20  50   100
               트래픽 수 (회)
```

**해석:**
- 초반 급격한 상승 (10~20회)
- 중반 안정적 상승 (20~50회)
- 후반 완만한 상승 (50~100회) → 한계 효용 체감

---

## 🔧 고급 활용

### 1. 특정 Phase만 분석

```bash
# Phase 1만 분석 (TC-001, TC-002, TC-003)
python scripts/analyze_results.py --tc-pattern "TC-00[123]"

# Phase 2만 분석 (TC-004 ~ TC-007)
python scripts/analyze_results.py --tc-pattern "TC-00[4567]"
```

### 2. 주간 보고서 자동 생성

```bash
# 전체 파이프라인 자동 실행 (Windows)
scripts/weekly_report.bat

# Linux/Mac
bash scripts/weekly_report.sh
```

**weekly_report.sh 내용:**
```bash
#!/bin/bash

# 1. 통계 분석
python scripts/analyze_results.py \
  --results-dir data/test_results \
  --output data/analysis/weekly_report_$(date +%Y%m%d).json

# 2. 차트 생성
python scripts/generate_charts.py \
  --report data/analysis/weekly_report_$(date +%Y%m%d).json \
  --output-dir data/charts/weekly_$(date +%Y%m%d)

# 3. HTML 보고서
python scripts/generate_report.py \
  --report data/analysis/weekly_report_$(date +%Y%m%d).json \
  --charts-dir data/charts/weekly_$(date +%Y%m%d) \
  --output data/reports/weekly_report_$(date +%Y%m%d).html

echo "✅ 주간 보고서 생성 완료: data/reports/weekly_report_$(date +%Y%m%d).html"
```

### 3. 실시간 대시보드 (추후 구현)

```python
# Streamlit 대시보드 (예시)
import streamlit as st

st.title("네이버 쇼핑 트래픽 테스트 대시보드")
st.metric("총 테스트 수", len(results))
st.metric("평균 순위 상승", f"{avg_rank_change:.1f}위")
st.bar_chart(platform_comparison)
```

---

## 📋 통계 분석 예시 출력

```
===================================================================================================
테스트 결과 종합 분석 보고서
===================================================================================================

=== 플랫폼별 효과 비교 분석 ===

모바일 100%:
  테스트 수: 3
  반복 횟수: 30
  평균 순위 변화: -3.80위
  표준편차: 2.10
  개선율: 73.3%
  효과 크기 (Cohen's d): 1.81

PC 100%:
  테스트 수: 3
  반복 횟수: 30
  평균 순위 변화: -1.20위
  표준편차: 1.50
  개선율: 46.7%
  효과 크기 (Cohen's d): 0.80

혼합 70:30:
  테스트 수: 3
  반복 횟수: 30
  평균 순위 변화: -3.20위
  표준편차: 1.90
  개선율: 68.0%
  효과 크기 (Cohen's d): 1.68

t-test (모바일 vs PC): t=5.123, p=0.0034
✅ 통계적으로 유의미한 차이 (p < 0.05)

=== 진입 경로별 효과 비교 분석 ===

통합검색:
  평균 순위 변화: -4.10위
  개선율: 75.0%

블로그:
  평균 순위 변화: -4.80위
  개선율: 82.0%

ANOVA (경로 비교): F=3.456, p=0.0289
✅ 경로 간 통계적으로 유의미한 차이 존재

=== 행동 패턴별 효과 비교 분석 ===

상관관계 (체류시간 vs 순위변화): r=-0.854, p=0.0021
✅ 강한 상관관계 (|r| > 0.6)

✅ 분석 보고서 저장: data/analysis/summary_report.json
```

---

## ⚠️ 주의사항

### 1. 데이터 품질
- 최소 3개 이상의 상품으로 테스트
- 각 테스트 케이스당 최소 10회 반복
- 봇 탐지로 인한 실패 건은 제외하고 분석

### 2. 통계적 유의성
- p-value < 0.05만 의미 있는 결과로 간주
- 샘플 수가 적으면 (< 30) 비모수 검정 고려
- 다중 비교 시 Bonferroni 보정 필요

### 3. 효과 크기 vs 통계적 유의성
- p-value만 보지 말고 효과 크기(Cohen's d)도 확인
- 실용적 의미가 있는지 판단 (1위 상승도 의미 있는가?)

### 4. 외부 변수 통제
- 테스트 기간 동안 경쟁사 변동 모니터링
- 네이버 알고리즘 업데이트 체크
- 요일/시간대 효과 고려

---

## 🔍 트러블슈팅

### 문제 1: 차트 생성 시 한글 깨짐

```python
# generate_charts.py 상단 수정
import matplotlib
matplotlib.rc('font', family='Malgun Gothic')  # Windows
# matplotlib.rc('font', family='AppleGothic')  # Mac
# matplotlib.rc('font', family='NanumGothic')  # Linux
```

### 문제 2: 분석 보고서 JSON 없음

```bash
# 결과 파일 확인
ls data/test_results/

# 테스트 결과가 없으면 먼저 실행
python run_test_case.py --tc TC-001 --products 1
```

### 문제 3: 통계 분석 에러

```
ValueError: not enough values to unpack
```

**원인**: 테스트 결과가 부족 (최소 2개 필요)

**해결**: 더 많은 테스트 실행 후 재분석

### 문제 4: HTML 보고서 차트 미표시

```
⚠️ 차트 없음: 1_platform_rank_change.png
```

**원인**: 차트 파일 경로 불일치

**해결**:
```bash
# 차트 디렉토리 확인
ls data/charts/

# 차트 재생성
python scripts/generate_charts.py
```

---

## 📚 관련 문서

- `TEST_PLAN.md` - 전체 테스트 계획 및 18개 테스트 케이스
- `run_test_case.py` - 테스트 케이스 실행 스크립트
- `REALISTIC_TEST_GUIDE.md` - 실제 트래픽 패턴 기반 테스트

---

## 🚀 전체 워크플로우 요약

```bash
# 1. Phase 1 테스트 실행 (플랫폼 비교)
python run_test_case.py --tc TC-001 --products 3  # 모바일
python run_test_case.py --tc TC-002 --products 3  # PC
python run_test_case.py --tc TC-003 --products 3  # 혼합

# 2. 통계 분석
python scripts/analyze_results.py

# 3. 차트 생성
python scripts/generate_charts.py

# 4. HTML 보고서
python scripts/generate_report.py

# 5. 브라우저에서 확인
start data/reports/final_report.html
```

**예상 소요 시간:**
- 테스트 실행: 4.5시간 (9개 테스트 × 30분)
- 분석 및 보고서 생성: 5분
- **총 소요: 약 5시간**

---

**작성일**: 2025-11-01
**버전**: 1.0
**핵심**: 통계적 신뢰성 확보 + 시각화된 인사이트 도출
