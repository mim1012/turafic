# Turafic C&C Server Dependencies

# FastAPI 및 웹 서버
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6  # 파일 업로드 지원

# 데이터베이스 (SQLAlchemy + AsyncIO)
sqlalchemy[asyncio]==2.0.25
aiosqlite==0.19.0  # SQLite 비동기 드라이버 (개발용)
asyncpg==0.29.0  # PostgreSQL 비동기 드라이버 (프로덕션용)

# Redis (선택 사항)
redis[hiredis]==5.0.1

# 데이터 검증
pydantic==2.5.3
pydantic-settings==2.1.0

# AI 비전 (자가 치유 시스템용, 선택 사항)
openai==1.10.0

# 유틸리티
python-dotenv==1.0.0  # 환경 변수 로드
