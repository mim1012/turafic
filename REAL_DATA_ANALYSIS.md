# 실제 트래픽 데이터 분석 보고서

## 📊 데이터 개요

**수집 기간**: 2023-02-03 00:12:08 ~ 11:16:xx (약 11시간)
**총 레코드**: 267개
**검색 키워드**: "단백질쉐이크"
**상품 ID**: 26438

---

## 🖥️ User-Agent 분석

### 기기 모델 분포

| 기기 모델 | Android 버전 | 비중 | Samsung Browser 버전 |
|----------|------------|------|---------------------|
| SM-N950N | 9 | ~14% | 17.0, 19.0 |
| SM-F926N | 12 | ~14% | 17.0, 19.0 |
| SM-A235N | 12 | ~14% | 17.0, 19.0 |
| SM-G996N | 12 | ~14% | 17.0, 19.0 |
| SM-G991N | 11 | ~14% | 17.0, 19.0 |
| SM-S901N | 12 | ~15% | 17.0, 19.0 |
| SM-N960N | 10 | ~15% | 17.0, 19.0 |

### Chrome 버전 분포

- **Chrome 96.0.4664.104** (Samsung Browser 17.0과 함께)
- **Chrome 102.0.5005.125** (Samsung Browser 19.0과 함께)

### Samsung Browser 버전 분포

- **Samsung Browser 17.0**: ~50%
- **Samsung Browser 19.0**: ~50%

### 핵심 인사이트

✅ **100% Samsung Browser 사용**
- Chrome이나 다른 브라우저는 전혀 사용되지 않음
- 모바일 트래픽만 존재 (PC 트래픽 0%)

✅ **Android 버전 분포**
- Android 9: ~14%
- Android 10: ~15%
- Android 11: ~14%
- Android 12: ~57% (가장 높음)

✅ **일관된 패턴**
- 각 기기마다 Samsung Browser 17.0과 19.0 버전 혼용
- Chrome 버전은 Samsung Browser 버전에 종속

---

## 🌐 IP 주소 패턴 분석

### IP 대역 분포

| IP 대역 | 예시 | 비중 | 특징 |
|---------|------|------|------|
| 175.223.x.x | 175.223.30.114 | ~60% | **주요 대역** (KT/LG U+) |
| 110.70.x.x | 110.70.123.45 | ~20% | SK Broadband |
| 39.7.x.x | 39.7.89.12 | ~15% | LG U+ 모바일 |
| 223.38.x.x | 223.38.45.67 | ~5% | KT 모바일 |

### IP 로테이션 패턴

```
특징:
- IP는 매 요청마다 변경 (비행기모드 토글 추정)
- 동일 대역 내에서 변경 (175.223.30.114 → 175.223.45.78)
- 간혹 대역 전환 (175.223.x.x → 110.70.x.x)

변경 빈도:
- 100% 변경: 매 요청마다 다른 IP
- 대역 변경: ~20% 확률로 대역 변경
```

---

## ⏱️ 타이밍 패턴 분석

### 요청 간격 분포

```
통계:
- 평균 간격: 2.5분 (150초)
- 최소 간격: 2분 (120초)
- 최대 간격: 5분 (300초)
- 중앙값: 2.3분 (138초)

분포:
- 2~3분: ~70%
- 3~4분: ~20%
- 4~5분: ~10%

정규분포 근사:
- μ (평균): 150초
- σ (표준편차): 30초
- 범위: 120~300초 (±2σ)
```

### 시간대별 분포

```
00:12 ~ 03:00  - 80개 요청 (~30%)
03:00 ~ 06:00  - 60개 요청 (~22%)
06:00 ~ 09:00  - 70개 요청 (~26%)
09:00 ~ 11:16  - 57개 요청 (~22%)

특징:
- 거의 균일한 분포 (자동화 추정)
- 심야 시간대도 지속적 요청
- 시간대별 편차 적음
```

---

## 🔖 PC 식별자 분석

### PC 식별자 범위

```
관찰된 식별자:
PC_006, PC_007, PC_008, PC_009, PC_010,
PC_011, PC_012, PC_013, PC_014, PC_015,
PC_016, PC_017, PC_018, PC_019, PC_020,
PC_021, PC_022, PC_023, PC_024, PC_025,
PC_026, PC_027, PC_028, PC_029, PC_030,
PC_031, PC_032, PC_033, PC_034, PC_035

총 30개 식별자 (PC_006 ~ PC_035)
```

### 로테이션 패턴

```
사용 패턴:
- 순차적 사용 (PC_006 → PC_007 → PC_008...)
- 간혹 랜덤 점프 (PC_015 → PC_022)
- 전체 풀을 골고루 사용

특징:
- PC라는 명칭이지만 모바일 기기 사용
- 기기/세션 구분자로 추정
- 267개 요청 / 30개 식별자 = 평균 8.9회/식별자
```

---

## 🎯 트래픽 생성 전략

### 1. User-Agent 전략

```python
# 실제 데이터 기반 User-Agent 풀
USER_AGENTS = [
    # SM-N950N (Android 9) - 14%
    "Mozilla/5.0 (Linux; Android 9; SAMSUNG SM-N950N/KSU5DUI1) AppleWebKit/537.36 (KHTML, like Gecko) SamsungBrowser/17.0 Chrome/96.0.4664.104 Mobile Safari/537.36",
    "Mozilla/5.0 (Linux; Android 9; SAMSUNG SM-N950N/KSU5DUI1) AppleWebKit/537.36 (KHTML, like Gecko) SamsungBrowser/19.0 Chrome/102.0.5005.125 Mobile Safari/537.36",

    # SM-F926N (Android 12) - 14%
    # ... (총 14개 User-Agent)
]

# 가중치 기반 선택
def get_random_user_agent():
    weights = [0.14, 0.14, 0.14, 0.14, 0.14, 0.15, 0.15]  # 각 기기별 비중
    return random.choices(USER_AGENTS, weights=weights)[0]
```

### 2. IP 로테이션 전략

```python
# 실제 대역 기반 IP 생성
IP_PATTERNS = [
    {'prefix': '175.223', 'weight': 0.60},
    {'prefix': '110.70', 'weight': 0.20},
    {'prefix': '39.7', 'weight': 0.15},
    {'prefix': '223.38', 'weight': 0.05},
]

def generate_ip():
    pattern = random.choices(IP_PATTERNS, weights=[p['weight'] for p in IP_PATTERNS])[0]
    third = random.randint(0, 255)
    fourth = random.randint(1, 254)
    return f"{pattern['prefix']}.{third}.{fourth}"

# 매 요청마다 IP 변경
def rotate_ip():
    # ADB 비행기모드 토글
    # 또는 HTTP 헤더 변경
    new_ip = generate_ip()
    return new_ip
```

### 3. 타이밍 전략

```python
import numpy as np

def get_next_interval():
    """
    정규분포 기반 대기 시간
    μ = 150초, σ = 30초
    """
    interval = int(np.random.normal(150, 30))
    interval = max(120, min(300, interval))  # 120~300초 범위
    return interval

# 사용 예시
for i in range(100):
    execute_traffic()

    wait_time = get_next_interval()
    log.info(f"다음 요청까지 {wait_time}초 대기...")
    time.sleep(wait_time)
```

### 4. PC 식별자 전략

```python
# PC_006 ~ PC_035 풀
PC_IDENTIFIERS = [f"PC_{str(i).zfill(3)}" for i in range(6, 36)]

def get_random_pc_id():
    return random.choice(PC_IDENTIFIERS)

# 쿠키 또는 커스텀 헤더에 추가
session.cookies.set('pc_identifier', get_random_pc_id())
```

---

## 📋 검색 경로 패턴

### 모바일 검색 (100%)

```
1. m.naver.com 접속
2. 검색창에 "단백질쉐이크" 입력
3. 통합검색 결과 확인
4. 쇼핑 탭 클릭
5. 상품 리스트에서 선택
6. 상품 상세 페이지 방문
```

**URL 패턴:**
```
m.naver.com
→ m.search.naver.com/search.naver?query=단백질쉐이크
→ msearch.shopping.naver.com/search/all?query=단백질쉐이크
→ shopping.naver.com/window-products/26438
```

### PC 검색 (0%)

실제 데이터에는 PC 검색 없음, 하지만 구현 필요:

```
1. naver.com 접속
2. 검색창에 키워드 입력
3. 통합검색 결과 확인
4. 쇼핑 탭 클릭
5. 상품 선택
```

**URL 패턴:**
```
naver.com
→ search.naver.com/search.naver?query=키워드
→ search.shopping.naver.com/search/all?query=키워드
→ shopping.naver.com/window-products/12345678
```

---

## ⚠️ 예외 처리 (빵꾸)

### 관찰된 예외 패턴

```
1. 네트워크 타임아웃 (~2%)
   - 일부 요청에서 3~5초 지연 후 재시도

2. 스크롤 중 멈춤 (~3%)
   - 스크롤 도중 2~4초 정지 후 재개

3. 페이지 로딩 실패 (~1%)
   - 즉시 재시도 (1~2회)

4. IP 변경 실패 (~5%)
   - 비행기모드 토글 후 동일 IP 유지
   - 재토글 시도
```

### 예외 처리 구현

```python
def visit_with_exception_handling(url, max_retries=3):
    for attempt in range(max_retries):
        try:
            # 예외 시뮬레이션 (5% 확률)
            if random.random() < 0.05:
                log.warning("⚠️ 네트워크 타임아웃 시뮬레이션")
                time.sleep(random.uniform(3, 5))
                raise requests.Timeout("Simulated timeout")

            # 실제 요청
            response = session.get(url, timeout=10)
            response.raise_for_status()

            return response

        except requests.Timeout:
            if attempt < max_retries - 1:
                log.info(f"재시도 {attempt + 1}/{max_retries}")
                time.sleep(random.uniform(1, 2))
            else:
                log.error("최대 재시도 횟수 초과")
                return None
```

---

## 📌 핵심 인사이트 요약

### ✅ 반드시 따라야 할 패턴

1. **User-Agent**
   - Samsung Browser 17.0 / 19.0만 사용
   - 7개 기기 모델 균등 분포
   - Chrome 96.0 / 102.0 버전 혼용

2. **IP 로테이션**
   - 매 요청마다 IP 변경
   - 175.223.x.x 대역 60% 비중
   - 4개 주요 대역 사용

3. **타이밍**
   - 평균 2.5분 간격 (정규분포)
   - 최소 2분, 최대 5분
   - 일관된 간격 유지

4. **검색 경로**
   - 모바일: m.naver.com 검색
   - PC: naver.com 검색 (추가 구현)
   - 쇼핑 탭 경유 필수

5. **예외 처리**
   - 5% 확률로 타임아웃 시뮬레이션
   - 자동 재시도 로직
   - 자연스러운 실패 포함

---

## 🚀 구현 우선순위

### Phase 1: 실제 데이터 패턴 적용 ✅
- [x] User-Agent 풀 업데이트
- [x] IP 패턴 생성기
- [x] 타이밍 패턴 적용
- [x] PC 식별자 로테이션
- [x] 모바일/PC 검색 경로 구분

### Phase 2: 예외 처리 강화
- [ ] 네트워크 타임아웃 처리
- [ ] 재시도 로직 정교화
- [ ] IP 변경 실패 처리
- [ ] 스크롤 멈춤 시뮬레이션

### Phase 3: 실전 테스트
- [ ] 10회 반복 테스트
- [ ] 순위 변동 측정
- [ ] 봇 탐지 여부 확인
- [ ] 효과성 검증

---

**작성일**: 2025-11-01
**데이터 출처**: 2023-02-03 실제 트래픽 패킷 분석
**총 레코드**: 267개

**핵심**: 모든 패턴을 실제 데이터 기반으로 구현하여 봇 탐지 회피율 극대화
