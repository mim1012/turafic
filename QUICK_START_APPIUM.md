# Appium 빠른 시작 가이드

**5분 안에 Appium 기반 트래픽 테스트 시작하기**

---

## 🚀 빠른 시작 (3단계)

### 1️⃣ Appium 설치 (5분)

```bash
# Node.js 설치 확인
node --version
# 없으면: https://nodejs.org 에서 LTS 버전 다운로드

# Appium 설치
npm install -g appium
appium driver install uiautomator2

# Python 클라이언트 설치
pip install Appium-Python-Client

# 환경 검증
appium-doctor --android
```

**모든 항목이 ✔ 표시되어야 합니다!**

---

### 2️⃣ 에뮬레이터 생성 (3분)

```bash
# Android Studio 설치 (이미 있다면 스킵)
# https://developer.android.com/studio

# 에뮬레이터 5개 생성 (자동 스크립트)
powershell -ExecutionPolicy Bypass -File scripts/create_emulators_quick.ps1
```

또는 수동 생성:
```bash
# AVD Manager 실행 (Android Studio)
# Tools → AVD Manager → Create Virtual Device
# 기기: Pixel 5
# OS: Android 12.0 (API 31)
# 이름: Emulator_PC_006, Emulator_PC_007, ...
```

---

### 3️⃣ 테스트 실행 (1분)

```bash
# 터미널 1: Appium 서버 시작
appium

# 터미널 2: 테스트 실행 (5개 에뮬레이터)
python run_appium_test.py --instances 5 --iterations 3
```

**끝!** 🎉

---

## 📊 실행 결과 예시

```
===================================================================================================
Appium 기반 트래픽 테스트 시작
에뮬레이터: 5개
반복 횟수: 3회
상품: OOO 블루투스 이어폰
===================================================================================================

[PC_006] 에뮬레이터 시작: Emulator_PC_006
[PC_007] 에뮬레이터 시작: Emulator_PC_007
...

✅ 모든 에뮬레이터 시작 완료 (5개)

[PC_006] Appium 연결 중...
[PC_007] Appium 연결 중...
...

✅ Appium 연결 완료: 5/5개

초기 순위: 52위

[1/3] 트래픽 생성
병렬 트래픽 생성 시작...
✅ 병렬 트래픽 생성 완료

순위 반영 대기: 30초

✅ 순위 상승: 4위 (52위 → 48위)

...

===================================================================================================
Appium 테스트 결과 요약
===================================================================================================

에뮬레이터: 5개
총 반복: 3회
평균 순위 변화: -3.67위
개선율: 100.0%
순위 상승: 3회
최대 상승: 4위

✅ 결과 저장: data/appium_results/appium_test_20250101_120000.json
```

---

## ⚙️ 옵션 설명

### 기본 사용법

```bash
python run_appium_test.py [옵션]
```

### 주요 옵션

| 옵션 | 설명 | 기본값 | 예시 |
|------|------|--------|------|
| `--instances` | 에뮬레이터 개수 | 5 | `--instances 10` |
| `--iterations` | 반복 횟수 | 10 | `--iterations 20` |
| `--product-id` | 테스트 상품 ID | 1 | `--product-id 2` |

### 사용 예시

```bash
# 1. 소규모 테스트 (5개 에뮬레이터, 3회 반복)
python run_appium_test.py --instances 5 --iterations 3

# 2. 중규모 테스트 (10개 에뮬레이터, 10회 반복)
python run_appium_test.py --instances 10 --iterations 10

# 3. 대규모 테스트 (27개 에뮬레이터, 50회 반복)
python run_appium_test.py --instances 27 --iterations 50

# 4. 다른 상품 테스트
python run_appium_test.py --product-id 2
```

---

## 🔧 리소스 요구사항

### 에뮬레이터 개수별 권장 사양

| 에뮬레이터 | CPU | RAM | 시간 (10회) |
|----------|-----|-----|------------|
| 5개 | 8코어 | 16GB | 약 1시간 |
| 10개 | 16코어 | 32GB | 약 1시간 |
| 27개 | 32코어 | 64GB | 약 1시간 |

**참고**: 에뮬레이터는 병렬 실행되므로 개수가 많아도 시간은 비슷합니다.

---

## 📁 결과 파일 위치

```
data/appium_results/
└── appium_test_20250101_120000.json
```

**결과 파일 구조**:
```json
{
  "test_start": "2025-01-01T12:00:00",
  "num_instances": 5,
  "iterations": [
    {
      "iteration": 1,
      "before_rank": 52,
      "after_rank": 48,
      "rank_change": -4
    }
  ],
  "summary": {
    "avg_rank_change": -3.67,
    "improvement_rate": 1.0
  }
}
```

---

## 🆚 HTTP vs Appium 비교

| 특징 | HTTP 방식 | Appium 방식 |
|------|----------|------------|
| **설정 시간** | 0분 | 10분 |
| **리소스** | 최소 | 높음 (에뮬레이터) |
| **IP 변경** | 헤더만 | 실제 변경 가능 |
| **봇 탐지 회피** | 중간 | 높음 |
| **권장 용도** | 프로토타입, 소규모 | 실전 배포, 대규모 |

**추천 전략**:
1. HTTP로 먼저 검증 (빠르고 간단)
2. 효과 확인 후 Appium으로 확장 (높은 효과)

---

## ❓ 자주 묻는 질문 (FAQ)

### Q1. Appium 서버가 시작되지 않아요

**A**: Node.js가 제대로 설치되었는지 확인:
```bash
node --version
npm --version

# 재설치 필요 시
npm install -g appium
```

---

### Q2. 에뮬레이터가 너무 느려요

**A**: Intel HAXM 또는 Windows Hypervisor 활성화:
```bash
# Intel CPU: HAXM 설치
# SDK Manager → Intel x86 Emulator Accelerator

# AMD CPU: Windows 기능 켜기
# Windows Hypervisor Platform 체크 → 재부팅
```

---

### Q3. "adb: command not found" 오류

**A**: 환경 변수 설정:
```bash
# 시스템 환경 변수 추가
ANDROID_HOME = C:\Users\사용자명\AppData\Local\Android\Sdk

# Path에 추가
%ANDROID_HOME%\platform-tools
```

---

### Q4. 에뮬레이터가 부팅되지 않아요

**A**: 가상화 지원 확인:
```bash
# BIOS에서 Intel VT-x 또는 AMD-V 활성화
# 작업 관리자 → 성능 탭 → 가상화: 사용

# Windows에서 Hyper-V 비활성화 (충돌 방지)
bcdedit /set hypervisorlaunchtype off
# 재부팅
```

---

### Q5. 27개 에뮬레이터를 동시에 실행할 수 없어요

**A**: 단계적 시작 (자동):
```python
# 코드가 자동으로 5개씩 단계적 시작
# batch_size=5, batch_delay=30초

# 또는 수동으로 10개씩
python run_appium_test.py --instances 10
```

---

## 🔍 트러블슈팅 체크리스트

문제 발생 시 다음을 순서대로 확인:

- [ ] `node --version` 정상 출력 (16.x 이상)
- [ ] `appium --version` 정상 출력
- [ ] `adb devices` 정상 출력
- [ ] `appium-doctor --android` 모두 ✔
- [ ] Appium 서버 실행 중 (`appium`)
- [ ] 에뮬레이터 부팅 완료 (30초~2분 소요)
- [ ] 환경 변수 `ANDROID_HOME` 설정됨

**모든 항목 체크 후에도 문제가 있다면**:
→ `APPIUM_SETUP_GUIDE.md` 상세 가이드 참조

---

## 📚 관련 문서

- `APPIUM_SETUP_GUIDE.md` - 상세 설치 가이드
- `PREVIOUS_IMPLEMENTATION.md` - 원본 방식 분석
- `run_appium_test.py` - 실행 스크립트 코드
- `src/automation/appium_farm.py` - 에뮬레이터 팜 코드

---

## 🎯 다음 단계

1. **소규모 테스트** (5개 에뮬레이터)
   ```bash
   python run_appium_test.py --instances 5 --iterations 3
   ```

2. **결과 분석**
   ```bash
   python scripts/analyze_results.py --results-dir data/appium_results
   ```

3. **스케일 업** (10개 → 27개)
   ```bash
   python run_appium_test.py --instances 27 --iterations 50
   ```

4. **자동화** (24시간 무인 운영)
   ```bash
   # 추후 구현: APScheduler 기반
   python run_scheduler.py --duration 24h
   ```

---

**작성일**: 2025-11-01
**난이도**: ⭐⭐ 중급
**소요 시간**: 설치 10분 + 테스트 5분 = 총 15분
**핵심**: 단계별로 천천히 따라하면 성공!
