# Turafic 전체 시스템 아키텍처

## 📋 시스템 개요

Turafic은 **C&C 서버 기반 분산 봇 네트워크**를 통해 네이버 쇼핑 트래픽 생성 및 순위 변동을 자동화하는 시스템입니다.

```
총 봇 수: 32대
├─ 트래픽 생성 봇: 24대 (3개 그룹 × 8봇)
└─ 순위 체크 봇: 8대 (1개 그룹 × 8봇)

각 그룹 구성:
├─ 대장 (Leader): 1대 - 핫스팟 제공, IP 관리
└─ 쫄병 (Minions): 5~7대 - 작업 실행, IP 공유
```

---

## 🏗️ 전체 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        관리자 대시보드 (Web UI)                          │
│                    실시간 모니터링 및 제어 인터페이스                      │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  - 봇 상태 모니터링                                                │  │
│  │  - 캠페인 관리                                                     │  │
│  │  - 순위 변동 그래프                                                │  │
│  │  - 그룹 Health Score 대시보드                                      │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼ HTTP API
┌─────────────────────────────────────────────────────────────────────────┐
│                       C&C 서버 (FastAPI)                                │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                       API 레이어 (7개 모듈)                        │  │
│  │  ┌────────────┬────────────┬────────────┬────────────┐           │  │
│  │  │Bot Mgmt API│Task API    │Campaign API│Ranking API │           │  │
│  │  └────────────┴────────────┴────────────┴────────────┘           │  │
│  │  ┌────────────┬────────────┬────────────┐                        │  │
│  │  │RankGroup   │Admin API   │             │                        │  │
│  │  │    API     │            │             │                        │  │
│  │  └────────────┴────────────┴────────────┘                        │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                       코어 레이어 (9개 모듈)                        │  │
│  │  ┌────────────────────────────────────────────────────────────┐  │  │
│  │  │ 작업 할당 엔진 (Task Engine)                                │  │  │
│  │  │ - "1봇 = 1캠페인 전담" 모델                                 │  │  │
│  │  │ - JSON 작업 패턴 생성                                       │  │  │
│  │  │ - 작업 시간 최적화 (max 90초)                               │  │  │
│  │  └────────────────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────────────────┐  │  │
│  │  │ 대장-쫄병 그룹 관리자 (Ranking Group Manager)               │  │  │
│  │  │ - Health Score 계산                                        │  │  │
│  │  │ - 쫄병 수 자동 조정 (5~7개)                                │  │  │
│  │  │ - 그룹 생성/삭제/조회                                       │  │  │
│  │  └────────────────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────────────────┐  │  │
│  │  │ IP 로테이션 관리자 (IP Rotation Manager)                   │  │  │
│  │  │ - 하이브리드 IP 변경 전략                                   │  │  │
│  │  │ - 작업 완료 대기 (최대 3분)                                │  │  │
│  │  │ - IP 변경 이력 추적                                        │  │  │
│  │  └────────────────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────────────────┐  │  │
│  │  │ Identity Profiles (신원 프로필 관리)                        │  │  │
│  │  │ - 15개 Samsung 기기 프로필                                 │  │  │
│  │  │ - User-Agent, Canvas Fingerprint 다양화                   │  │  │
│  │  └────────────────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────────────────┐  │  │
│  │  │ HTTP 패턴 생성기 (HTTP Pattern Generator)                  │  │  │
│  │  │ - 브라우저 행동 시뮬레이션                                  │  │  │
│  │  │ - 자연스러운 요청 간격                                      │  │  │
│  │  └────────────────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────────────────┐  │  │
│  │  │ 백그라운드 작업 스케줄러 (Background Tasks)                │  │  │
│  │  │ - IP 로테이션 체크 (30초 주기)                             │  │  │
│  │  │ - 헬스 체크 및 쫄병 수 조정 (5분 주기)                     │  │  │
│  │  │ - 순위 체크 스케줄링 (12시간 주기)                          │  │  │
│  │  └────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                       데이터 레이어                                │  │
│  │  ┌─────────────┐  ┌─────────────┐                               │  │
│  │  │ PostgreSQL  │  │   Redis     │                               │  │
│  │  │ (주 DB)     │  │ (캐시/세션) │                               │  │
│  │  └─────────────┘  └─────────────┘                               │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼ REST API (HTTP/JSON)
┌─────────────────────────────────────────────────────────────────────────┐
│                       봇 네트워크 (32대)                                 │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────┐     │
│  │              Traffic Group 1 (8봇)                            │     │
│  │  ┌──────────────────────────────────────────────────────┐     │     │
│  │  │ 대장 봇 (Leader)                                     │     │     │
│  │  │ - 핫스팟 ON (SSID: Traffic-G1)                       │     │     │
│  │  │ - IP 관리: 192.168.43.1                              │     │     │
│  │  │ - Health: Battery 75%, Temp 35°, Score 87.5         │     │     │
│  │  └──────────────────────────────────────────────────────┘     │     │
│  │         │                                                      │     │
│  │    ┌────┴─────────┬────────┬────────┬────────┬────────┐       │     │
│  │    ▼      ▼       ▼        ▼        ▼        ▼        ▼       │     │
│  │  ┌────┐ ┌────┐ ┌────┐  ┌────┐  ┌────┐  ┌────┐  ┌────┐      │     │
│  │  │쫄병│ │쫄병│ │쫄병│  │쫄병│  │쫄병│  │쫄병│  │쫄병│      │     │
│  │  │ #1 │ │ #2 │ │ #3 │  │ #4 │  │ #5 │  │ #6 │  │ #7 │      │     │
│  │  └────┘ └────┘ └────┘  └────┘  └────┘  └────┘  └────┘      │     │
│  │  IP: 192.168.43.2~8 (대장 핫스팟 연결)                       │     │
│  │  작업: TC#001~009 (L18 테스트 케이스)                        │     │
│  └───────────────────────────────────────────────────────────────┘     │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────┐     │
│  │              Traffic Group 2 (8봇)                            │     │
│  │  [구조 동일 - 생략]                                           │     │
│  └───────────────────────────────────────────────────────────────┘     │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────┐     │
│  │              Traffic Group 3 (8봇)                            │     │
│  │  [구조 동일 - 생략]                                           │     │
│  └───────────────────────────────────────────────────────────────┘     │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────┐     │
│  │              Rank Checker Group (8봇)                         │     │
│  │  - 대장 1 + 쫄병 7                                            │     │
│  │  - 순위 체크 전담 (12시간 주기)                               │     │
│  └───────────────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼ Appium / Root 제어
┌─────────────────────────────────────────────────────────────────────────┐
│                         네이버 쇼핑                                      │
│                   (트래픽 생성 대상 서비스)                              │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ 검색 결과 페이지                                                  │  │
│  │ 1. 광고 상품 (제외)                                               │  │
│  │ 2. 일반 상품 #1 ← 봇 트래픽                                      │  │
│  │ 3. 일반 상품 #2                                                   │  │
│  │ ...                                                               │  │
│  │ 20. 일반 상품 #20                                                 │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🗂️ 데이터베이스 스키마

### 핵심 테이블 (11개)

```sql
┌──────────────────────────────────────────────────────────────┐
│                     bots (봇 정보)                           │
├──────────────────────────────────────────────────────────────┤
│ bot_id (PK)                                                  │
│ android_id (UNIQUE)                                          │
│ device_model                                                 │
│ bot_type                     -- 'traffic' or 'rank_checker'  │
│ is_leader                    -- TRUE/FALSE                   │
│ leader_bot_id (FK → bots)    -- 쫄병인 경우 대장 ID         │
│ ranking_group_id (FK)        -- 소속 그룹                    │
│ health_score                 -- 0~100                        │
│ battery_level                -- 0~100%                       │
│ device_temperature           -- 섭씨                         │
│ task_status                  -- 'idle', 'working', 'completed'│
│ current_ip                   -- 현재 IP 주소                 │
│ ...                                                          │
└──────────────────────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────────────┐
│              ranking_groups (대장-쫄병 그룹)                 │
├──────────────────────────────────────────────────────────────┤
│ group_id (PK)                                                │
│ group_name                                                   │
│ group_type                   -- 'traffic' or 'rank_checker'  │
│ leader_bot_id (FK → bots)    -- 대장 봇 ID                  │
│ min_minions                  -- 최소 5                       │
│ max_minions                  -- 최대 7                       │
│ target_minion_count          -- 목표 쫄병 수 (기본 7)        │
│ current_minion_count         -- 현재 쫄병 수                 │
│ ip_change_strategy           -- 'wait_for_completion'        │
│ ip_change_interval_sec       -- 300초 (5분)                  │
│ max_wait_time_sec            -- 180초 (3분)                  │
│ current_ip                   -- 그룹 공유 IP                 │
│ status                       -- 'active', 'paused'           │
│ ...                                                          │
└──────────────────────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────────────┐
│                campaigns (캠페인 정보)                       │
├──────────────────────────────────────────────────────────────┤
│ campaign_id (PK)                                             │
│ name                                                         │
│ target_keyword                -- "프로틴 쉐이크"             │
│ target_traffic                -- 목표 트래픽 수 (100)        │
│ test_case                     -- "TC#001"                    │
│ execution_mode                -- 'appium' or 'http'          │
│ identity_profile_group        -- 'samsung_mobile_default'    │
│ assigned_bot_id (FK → bots)   -- 1봇 = 1캠페인              │
│ status                        -- 'active', 'completed'       │
│ current_traffic_count         -- 진행률                      │
│ ...                                                          │
└──────────────────────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────────────┐
│                   tasks (작업 정보)                          │
├──────────────────────────────────────────────────────────────┤
│ task_id (PK)                                                 │
│ bot_id (FK → bots)                                           │
│ campaign_id (FK → campaigns)                                 │
│ pattern (JSON)               -- 작업 패턴                    │
│ status                       -- 'assigned', 'running', ...   │
│ created_at                                                   │
│ completed_at                                                 │
│ ...                                                          │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│            ip_change_history (IP 변경 이력)                  │
├──────────────────────────────────────────────────────────────┤
│ id (PK)                                                      │
│ group_id (FK → ranking_groups)                               │
│ old_ip                                                       │
│ new_ip                                                       │
│ change_reason                -- 'all_completed', 'timeout'   │
│ minions_completed            -- 완료한 쫄병 수               │
│ minions_total                -- 전체 쫄병 수                 │
│ wait_duration_sec            -- 대기 시간                    │
│ changed_at                                                   │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│       task_completion_signals (작업 완료 신호)              │
├──────────────────────────────────────────────────────────────┤
│ signal_id (PK)                                               │
│ group_id (FK → ranking_groups)                               │
│ bot_id (FK → bots)                                           │
│ task_id                                                      │
│ completed_at                                                 │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│          ranking_checks (순위 체크 기록)                     │
├──────────────────────────────────────────────────────────────┤
│ check_id (PK)                                                │
│ campaign_id (FK)                                             │
│ keyword                                                      │
│ rank_position                -- 순위 (1부터)                 │
│ page_number                  -- 페이지 번호                  │
│ check_type                   -- 'baseline', 'batch_1', ...   │
│ measured_at                                                  │
│ ...                                                          │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│         ranking_changes (순위 변동 분석)                     │
├──────────────────────────────────────────────────────────────┤
│ change_id (PK)                                               │
│ campaign_id (FK)                                             │
│ before_rank                                                  │
│ after_rank                                                   │
│ rank_change                  -- 음수 = 상승                  │
│ test_case_id                 -- "TC#001"                     │
│ ...                                                          │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 작업 프로세스 플로우

### 1. 봇 등록 프로세스

```
Android 봇 실행
    ↓
기기 정보 수집 (android_id, 모델명, 해상도)
    ↓
POST /api/v1/bots/register
    ↓
서버: bot_id 생성 및 그룹 할당
    ↓
봇: bot_id 저장 및 백그라운드 서비스 시작
    ↓
대장 봇: 핫스팟 활성화
쫄병 봇: 대장 핫스팟 연결 시도
    ↓
5초마다 GET /api/v1/tasks/get_task 요청
```

### 2. 작업 할당 및 실행 프로세스

```
봇: GET /api/v1/tasks/get_task?bot_id=xxx
    ↓
서버: 캠페인 할당 확인
    ├─ 미할당 → 새 캠페인 할당 (1봇 = 1캠페인)
    └─ 할당됨 → 기존 캠페인 계속
    ↓
서버: JSON 작업 패턴 생성
    ├─ Identity Profile 적용
    ├─ 무작위성 추가 (±10px, ±20%)
    └─ 작업 시간 제한 (max 90초)
    ↓
봇: JSON 패턴 수신 및 실행
    ├─ Appium 모드: UI 자동화
    └─ HTTP 모드: 브라우저 시뮬레이션
    ↓
봇: 작업 완료 신호 전송
    POST /api/v1/ranking-groups/{group_id}/tasks/complete
    ↓
서버: task_status = 'completed' 업데이트
    ↓
모든 쫄병 완료 시 IP 변경 준비
```

### 3. IP 로테이션 프로세스 (하이브리드 전략)

```
[백그라운드 작업: 30초 주기]
    ↓
각 그룹에 대해 IP 변경 시점 체크
    ├─ 경과 시간 확인 (마지막 IP 변경 후)
    ├─ 쫄병 작업 상태 확인
    │   ├─ working_count = 0 (모두 완료)
    │   └─ working_count > 0 (작업 중)
    └─ 조건 판단
        ├─ [Case 1] 모든 쫄병 완료 + 5분 경과
        │   → IP 변경 실행 (reason: all_completed)
        ├─ [Case 2] 3분 대기 초과
        │   → 강제 IP 변경 (reason: timeout)
        └─ [Case 3] 아직 대기 중
            → IP 변경 없음
    ↓
IP 변경 실행
    ├─ 대장 봇: 비행기 모드 ON (3초)
    ├─ 대장 봇: 비행기 모드 OFF
    ├─ 네트워크 재연결 대기 (최대 10초)
    ├─ 새 IP 획득 및 DB 업데이트
    ├─ IP 변경 이력 저장
    └─ 모든 쫄병 task_status = 'idle' 리셋
    ↓
다음 작업 사이클 시작
```

### 4. 헬스 체크 및 쫄병 수 조정 프로세스

```
[백그라운드 작업: 5분 주기]
    ↓
대장 봇 헬스 정보 조회
    ├─ 배터리 레벨
    ├─ 기기 온도
    ├─ 메모리 가용량
    ├─ 핫스팟 안정성
    └─ 네트워크 지연
    ↓
Health Score 계산 (0~100)
    배터리×30% + 메모리×25% + 핫스팟×25% + 네트워크×15% + 온도×5%
    ↓
쫄병 수 결정
    ├─ 정상 (battery ≥30%, temp ≤40°) → 7개
    ├─ 경고 (battery <30%, temp >40°) → 6개
    └─ 위험 (battery <15%, temp >45°) → 5개
    ↓
현재 쫄병 수와 비교
    ├─ 동일 → 조정 없음
    └─ 다름 → 자동 조정
        ├─ 증가 → 미할당 봇 추가
        └─ 감소 → idle 봇부터 제거
    ↓
DB 업데이트 및 로그 기록
```

### 5. 순위 체크 프로세스 (12시간 주기)

```
[백그라운드 작업: 12시간 주기]
    ↓
Rank Checker Group 활성화
    ↓
대장 + 쫄병 7대가 병렬로 순위 체크
    ├─ 네이버 쇼핑 검색 (keyword)
    ├─ 광고 필터링 (8가지 패턴)
    ├─ 페이지별 스캔 (1~10페이지)
    └─ 상품 발견 시 순위 계산
        rank = (page - 1) × 20 + position
    ↓
POST /api/v1/ranking/report
    {
        "rank_position": 28,
        "page_number": 2,
        "check_type": "batch_1"
    }
    ↓
서버: ranking_checks 테이블에 저장
    ↓
이전 순위와 비교 → ranking_changes 저장
    rank_change = after_rank - before_rank
    (음수 = 상승, 양수 = 하락)
    ↓
다음 12시간 후 재측정
```

---

## 🧩 핵심 모듈 설명

### 1. Task Engine (작업 할당 엔진)

**파일**: `server/core/task_engine.py`

**책임**:
- "1봇 = 1캠페인 전담" 모델 구현
- JSON 작업 패턴 생성
- 무작위성 추가 (탐지 회피)
- 작업 시간 최적화 (IP 충돌 방지)

**주요 함수**:
```python
def generate_task_pattern(task_config, coordinates, keyword):
    """
    작업 패턴 생성
    - High: 60~90초
    - Medium: 45~60초
    - Low: 30~45초
    """

def add_randomness_to_pattern(pattern):
    """
    탐지 회피를 위한 무작위성 추가
    - 좌표 ±10px
    - 시간 ±20%
    """
```

---

### 2. Ranking Group Manager (대장-쫄병 관리자)

**파일**: `server/core/ranking_group_manager.py`

**책임**:
- 그룹 생성/삭제/조회
- 쫄병 할당/제거
- Health Score 계산
- 쫄병 수 자동 조정

**주요 함수**:
```python
def calculate_health_score(battery, memory, hotspot, network, temp):
    """
    Health Score = 배터리×30% + 메모리×25% + 핫스팟×25%
                  + 네트워크×15% + 온도×5%
    """

def determine_minion_count(battery, temp, health_score):
    """
    보수적 정책:
    - 정상: 7개
    - 경고: 6개
    - 위험: 5개
    """

async def adjust_minion_count(group_id, force=False):
    """
    쫄병 수 자동 조정
    - 증가 시: 미할당 봇 추가
    - 감소 시: idle 봇부터 제거
    """
```

---

### 3. IP Rotation Manager (IP 로테이션 관리자)

**파일**: `server/core/ip_rotation_manager.py`

**책임**:
- IP 변경 시점 판단
- 하이브리드 전략 구현
- IP 변경 이력 추적
- 작업 완료 신호 처리

**주요 함수**:
```python
async def should_change_ip(group_id):
    """
    IP 변경 시점 판단
    1. 모든 쫄병 완료 + 5분 경과 → 즉시 변경
    2. 3분 대기 초과 → 강제 변경
    3. 아직 대기 중 → 변경 없음
    """

async def execute_ip_change(group_id, reason, wait_duration, ...):
    """
    IP 변경 실행
    1. 비행기 모드 토글
    2. 새 IP 획득
    3. DB 업데이트
    4. 쫄병 task_status 리셋
    """
```

---

### 4. Identity Profiles (신원 프로필 관리)

**파일**: `server/core/identity_profiles.py`

**책임**:
- 15개 Samsung 기기 프로필 관리
- User-Agent 다양화
- Canvas Fingerprint 생성
- WebGL Vendor 정보 제공

**데이터 구조**:
```python
identity_profiles = [
    {
        "profile_id": "samsung_s24_ultra_1",
        "device_model": "SM-S928N",
        "user_agent": "Mozilla/5.0 (Linux; Android 14; SM-S928N)...",
        "screen_resolution": "1440x3088",
        "fingerprint": {
            "canvas": "hash_value_1",
            "webgl": "Adreno (TM) 750"
        }
    },
    # ... 14개 더
]
```

---

### 5. Background Tasks (백그라운드 작업 스케줄러)

**파일**: `server/core/background_tasks.py`

**책임**:
- APScheduler 초기화
- IP 로테이션 자동 체크 (30초 주기)
- 헬스 체크 및 쫄병 수 조정 (5분 주기)
- 순위 체크 스케줄링 (12시간 주기)

**스케줄**:
```python
# 30초마다 IP 로테이션 체크
scheduler.add_job(
    func=ip_rotation_check_job,
    trigger=IntervalTrigger(seconds=30),
    id="ip_rotation_check"
)

# 5분마다 헬스 체크 및 쫄병 수 조정
scheduler.add_job(
    func=health_check_and_adjust_job,
    trigger=IntervalTrigger(minutes=5),
    id="health_check_adjust"
)
```

---

## 📡 REST API 엔드포인트 맵

```
BASE_URL: http://localhost:8000

┌──────────────────────────────────────────────────────────────┐
│                    Bot Management API                        │
├──────────────────────────────────────────────────────────────┤
│ POST   /api/v1/bots/register         - 봇 등록              │
│ GET    /api/v1/bots/{bot_id}         - 봇 정보 조회         │
│ GET    /api/v1/bots/list             - 전체 봇 목록         │
│ PUT    /api/v1/bots/{bot_id}/status  - 봇 상태 업데이트     │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                    Task Assignment API                       │
├──────────────────────────────────────────────────────────────┤
│ GET    /api/v1/tasks/get_task        - 작업 요청            │
│ POST   /api/v1/tasks/report_result   - 결과 보고            │
│ GET    /api/v1/tasks/{task_id}       - 작업 상태 조회       │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                   Campaign Management API                    │
├──────────────────────────────────────────────────────────────┤
│ POST   /api/v1/campaigns/create      - 캠페인 생성          │
│ GET    /api/v1/campaigns/list        - 캠페인 목록          │
│ GET    /api/v1/campaigns/{id}        - 캠페인 상세          │
│ PUT    /api/v1/campaigns/{id}/status - 캠페인 상태 변경     │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                   Ranking Group API (NEW)                    │
├──────────────────────────────────────────────────────────────┤
│ POST   /api/v1/ranking-groups/groups/create                  │
│        - 대장-쫄병 그룹 생성                                  │
│                                                              │
│ POST   /api/v1/ranking-groups/groups/{id}/minions/assign     │
│        - 쫄병 할당                                           │
│                                                              │
│ DELETE /api/v1/ranking-groups/groups/{id}/minions/{bot_id}   │
│        - 쫄병 제거                                           │
│                                                              │
│ POST   /api/v1/ranking-groups/groups/{id}/leader/health      │
│        - 대장 헬스 정보 업데이트                             │
│                                                              │
│ POST   /api/v1/ranking-groups/groups/{id}/adjust             │
│        - 쫄병 수 자동 조정                                   │
│                                                              │
│ GET    /api/v1/ranking-groups/groups/{id}/status             │
│        - 그룹 상태 조회                                      │
│                                                              │
│ POST   /api/v1/ranking-groups/groups/{id}/ip/check           │
│        - IP 변경 시점 체크                                   │
│                                                              │
│ POST   /api/v1/ranking-groups/groups/{id}/ip/execute         │
│        - IP 변경 즉시 실행                                   │
│                                                              │
│ POST   /api/v1/ranking-groups/groups/{id}/tasks/complete     │
│        - 쫄병 작업 완료 보고                                 │
│                                                              │
│ GET    /api/v1/ranking-groups/groups/list                    │
│        - 전체 그룹 목록 조회                                 │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                      Ranking API                             │
├──────────────────────────────────────────────────────────────┤
│ POST   /api/v1/ranking/request_check - 순위 체크 요청       │
│ POST   /api/v1/ranking/report        - 순위 보고            │
│ GET    /api/v1/ranking/campaigns/{id}/history                │
│        - 순위 이력 조회                                      │
│ GET    /api/v1/ranking/campaigns/{id}/current                │
│        - 현재 순위 조회                                      │
│ GET    /api/v1/ranking/dashboard/overview                    │
│        - 전체 캠페인 순위 현황                               │
│ GET    /api/v1/ranking/analyze       - ANOVA 분석           │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                      Admin Dashboard API                     │
├──────────────────────────────────────────────────────────────┤
│ GET    /api/v1/admin/dashboard       - 대시보드 요약        │
│ GET    /api/v1/admin/bots/stats      - 봇 통계              │
│ GET    /api/v1/admin/campaigns/stats - 캠페인 통계          │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                        System API                            │
├──────────────────────────────────────────────────────────────┤
│ GET    /                             - 서버 상태 확인        │
│ GET    /health                       - 헬스 체크            │
│ GET    /docs                         - API 문서 (Swagger)   │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔐 보안 및 안티 탐지 전략

### 1. Identity Profiles (신원 프로필 다양화)

```
15개 Samsung 기기 프로필
├─ User-Agent 다양화
├─ Canvas Fingerprint 15가지
├─ WebGL Vendor 정보 다양화
└─ 화면 해상도 다양화 (5가지)
```

### 2. 행동 패턴 무작위화

```
좌표 무작위화: ±10px
시간 무작위화: ±20%
스크롤 속도: 100~300ms
체류 시간: 정규분포 랜덤
```

### 3. IP 로테이션

```
주기: 5분 (300초)
방식: 비행기 모드 토글
충돌 방지: 작업 완료 대기 (최대 3분)
```

### 4. 자연스러운 행동 시뮬레이션

```
Bezier Curve 터치 궤적
정규분포 클릭 간격
실제 사용자처럼 불규칙한 스크롤
페이지 체류 시간 다양화
```

---

## 📊 모니터링 및 통계

### 실시간 대시보드 지표

```javascript
{
    // 봇 상태
    "total_bots": 32,
    "active_bots": 32,
    "inactive_bots": 0,

    // 그룹 상태
    "total_groups": 4,
    "traffic_groups": 3,
    "rank_checker_groups": 1,

    // 헬스 스코어
    "average_health_score": 85.3,
    "leaders_with_low_battery": 0,
    "leaders_with_high_temp": 1,

    // IP 로테이션
    "ip_changes_today": 120,
    "average_ip_change_interval": 325,  // 초
    "timeout_ip_changes": 5,
    "completed_ip_changes": 115,

    // 쫄병 수 조정
    "minion_adjustments_today": 8,
    "average_minion_count": 6.8,

    // 작업 통계
    "total_tasks_today": 2400,
    "successful_tasks": 2350,
    "failed_tasks": 50,
    "average_task_duration": 75,  // 초

    // 순위 변동
    "baseline_rank": 52,
    "current_rank": 28,
    "rank_improvement": -24
}
```

---

## 🚀 배포 및 확장

### 인프라 구성

```
┌─────────────────────────────────────────┐
│         Oracle Cloud (무료 티어)        │
├─────────────────────────────────────────┤
│ - VM.Standard.A1.Flex (ARM)             │
│ - 4 OCPU, 24GB RAM                      │
│ - Ubuntu 22.04 LTS                      │
│ - PostgreSQL 14                         │
│ - Redis 7                               │
│ - Nginx (리버스 프록시)                 │
└─────────────────────────────────────────┘
                │
                ▼ HTTPS
┌─────────────────────────────────────────┐
│          FastAPI 서버 (Uvicorn)         │
│          8개 워커 프로세스              │
└─────────────────────────────────────────┘
                │
                ▼ 4G/5G
┌─────────────────────────────────────────┐
│         Android 봇 네트워크 (32대)      │
│  - 각 봇: Samsung Galaxy S7~S24         │
│  - Root 권한 (Magisk)                   │
│  - 24/7 백그라운드 실행                 │
└─────────────────────────────────────────┘
```

### 확장성

```
현재 용량: 32봇
├─ Traffic: 24봇 (3그룹)
└─ Ranking: 8봇 (1그룹)

확장 가능:
├─ Traffic: 최대 243봇 (L18 × 18 = 243 케이스)
└─ Ranking: 필요에 따라 추가 그룹
```

---

## 📚 관련 문서

1. [CLAUDE.md](../CLAUDE.md) - 프로젝트 전체 개요
2. [IP_ROTATION_STRATEGY.md](./IP_ROTATION_STRATEGY.md) - IP 로테이션 상세 가이드
3. [RANKING_GROUP_GUIDE.md](./RANKING_GROUP_GUIDE.md) - 대장-쫄병 시스템 사용법
4. [RANKING_SYSTEM_GUIDE.md](./RANKING_SYSTEM_GUIDE.md) - 순위 체크 시스템
5. [IMPLEMENTATION_GUIDE.md](./IMPLEMENTATION_GUIDE.md) - 구현 가이드

---

**작성일**: 2025-11-02
**버전**: 2.0.0
**상태**: ✅ 대장-쫄병 시스템 통합 완료
