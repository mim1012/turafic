# 구현 격차 분석 (빵꾸 찾기)

## ✅ 구현 완료된 모듈

### 1. 순위 체크 시스템
- [x] `src/ranking/checker.py` - 네이버 쇼핑 순위 크롤링
- [x] `src/ranking/tracker.py` - 순위 히스토리 저장/분석
- [x] `test_rank_checker.py` - 테스트 스크립트

**상태**: 완료 ✅
**문제점**: HTML 선택자가 네이버 변경 시 업데이트 필요

---

### 2. ADB 모바일 제어
- [x] `src/automation/mobile.py` - ADB 기본 제어
  - [x] 비행기모드 토글
  - [x] 화면 탭/스와이프
  - [x] 텍스트 입력
  - [x] URL 열기
- [x] `test_adb_controller.py` - 테스트 스크립트

**상태**: 완료 ✅
**문제점**: 없음

---

### 3. 분석 시스템
- [x] `src/analysis/case_comparison.py` - 케이스별 비교 분석
- [x] `TEST_CASES.md` - 테스트 케이스 정의
- [x] `TEST_STRATEGY.md` - 전략 재설계

**상태**: 완료 ✅
**문제점**: 그룹 기반 분석 로직 미구현

---

### 4. 유틸리티
- [x] `src/utils/logger.py` - 로깅 시스템
- [x] `src/utils/helpers.py` - 헬퍼 함수
- [x] `config/settings.py` - 설정 관리

**상태**: 완료 ✅

---

## ❌ 빠진 핵심 모듈

### 1. 브라우저 자동화 모듈 ⚠️ **최우선**
```
파일: src/automation/browser.py
상태: 미구현 ❌

필요 기능:
- [ ] 네이버 메인 접속
- [ ] 검색창에 키워드 입력
- [ ] 쇼핑탭 클릭
- [ ] 스크롤하여 상품 찾기
- [ ] 상품 클릭 → 상세페이지 진입
- [ ] 상세페이지에서 스크롤
- [ ] 장바구니/리뷰/문의/옵션 클릭
- [ ] 체류 시간 대기

구현 방법:
1. Pure ADB Shell (좌표 기반)
2. Appium + uiautomator2 (권장)
3. Chrome DevTools Protocol

중요도: ★★★★★
```

---

### 2. 시나리오 실행 모듈 ⚠️ **최우선**
```
파일: src/automation/scenario_executor.py
상태: 미구현 ❌

필요 기능:
- [ ] CASE_1 시나리오 실행 (적극적 구매)
- [ ] CASE_2 시나리오 실행 (정보 탐색)
- [ ] CASE_3 시나리오 실행 (쇼핑몰 직접)
- [ ] CASE_4 시나리오 실행 (빠른 비교)
- [ ] CASE_5 시나리오 실행 (문의형)
- [ ] 케이스별 Variation 적용
- [ ] 확률 기반 케이스 선택

구조:
class ScenarioExecutor:
    def execute_case_1(self, product):
        # 네이버 검색 → 쇼핑탭 → 리뷰 스크롤 → 장바구니
        pass

    def execute_case_2(self, product):
        # 네이버 검색 → 쇼핑탭 → 리뷰+Q&A 스크롤 → 리뷰 클릭
        pass

    def select_case_by_probability(self, distribution):
        # 확률 기반 케이스 선택
        pass

중요도: ★★★★★
```

---

### 3. 메인 실행 스크립트 ⚠️ **최우선**
```
파일: main.py
상태: 미구현 ❌

필요 기능:
- [ ] test_products.json 로드
- [ ] 각 상품별 100회 반복 실행
- [ ] Before/After 순위 체크
- [ ] 시나리오 실행 (케이스 확률 선택)
- [ ] ADB 비행기모드 토글
- [ ] 결과 저장 (executed_case 포함)
- [ ] 에러 핸들링 및 재시도
- [ ] 진행 상황 로깅

구조:
def main():
    # 1. 초기화
    load_config()
    load_test_products()

    # 2. 각 상품 처리
    for product in products:
        for i in range(100):
            # Before 순위
            rank_before = check_rank(product)

            # 케이스 선택 및 실행
            case = select_case_probabilistic(product.group)
            execute_scenario(product, case)

            # IP 변경
            toggle_airplane_mode()

            # After 순위
            rank_after = check_rank(product)

            # 저장
            save_result(product, i, case, rank_before, rank_after)

중요도: ★★★★★
```

---

### 4. 데이터 구조 업데이트 ⚠️ **중요**

#### 4.1. test_products.json 구조 변경
```
현재 상태: 케이스별 구조
필요 상태: 그룹 기반 구조 (확률 분포 포함)

변경 필요:
{
  "product_groups": [
    {
      "group_id": "GROUP_A",
      "primary_case": "CASE_1",
      "case_distribution": {
        "CASE_1": 0.50,
        "CASE_2": 0.20,
        ...
      },
      "products": [...]
    }
  ]
}

중요도: ★★★★☆
```

#### 4.2. tracker.py 업데이트
```
파일: src/ranking/tracker.py
상태: 부분 구현 ⚠️

필요 추가:
- [ ] add_record()에 executed_case 파라미터 추가
- [ ] 케이스별 필터링 통계 함수 추가
- [ ] get_stats_by_case(case_id) 함수 추가

def add_record(self, rank_info, iteration, test_case_id, executed_case, notes=""):
    record = {
        ...
        "executed_case": executed_case,  # ← 추가 필요
        ...
    }

def get_stats_by_case(self, case_id):
    # 특정 케이스만 실행한 iteration 필터링
    case_records = [r for r in self.history if r.get('executed_case') == case_id]
    # 통계 계산
    ...

중요도: ★★★★☆
```

#### 4.3. case_comparison.py 업데이트
```
파일: src/analysis/case_comparison.py
상태: 부분 구현 ⚠️

필요 추가:
- [ ] 그룹 기반 분석 로직
- [ ] get_products_by_group(group_id)
- [ ] calculate_group_statistics(group_id)
- [ ] compare_groups() - 그룹 간 비교

def calculate_group_statistics(self, group_id):
    # 그룹 내 상품들의 평균 순위 변동
    # 주력 케이스의 효과성 분석
    ...

중요도: ★★★★☆
```

---

### 5. 에러 처리 및 복구 로직 ⚠️ **중요**
```
파일: src/utils/error_handler.py
상태: 미구현 ❌

필요 기능:
- [ ] 순위 체크 실패 시 재시도 (최대 3회)
- [ ] ADB 연결 끊김 시 재연결
- [ ] 네트워크 타임아웃 처리
- [ ] 에러 발생 시 스크린샷 저장
- [ ] 중단된 지점부터 재개 (resume)

class ErrorHandler:
    def handle_rank_check_failure(self, product):
        # 재시도 로직
        pass

    def handle_adb_disconnect(self):
        # 재연결 시도
        pass

    def save_error_screenshot(self, error_type):
        # 스크린샷 저장
        pass

    def save_checkpoint(self, product, iteration):
        # 중단 지점 저장
        pass

    def resume_from_checkpoint(self):
        # 중단된 지점부터 재개
        pass

중요도: ★★★★☆
```

---

### 6. 환경 검증 및 초기화 ⚠️ **중요**
```
파일: src/utils/validator.py
상태: 미구현 ❌

필요 기능:
- [ ] .env 파일 필수 값 체크
- [ ] ADB 연결 상태 확인
- [ ] test_products.json 유효성 검증
- [ ] 기기 배터리/온도 체크
- [ ] 디렉토리 구조 생성

class EnvironmentValidator:
    def validate_env_file(self):
        # ADB_DEVICE_ID, AUTOMATION_MODE 등 필수 값 체크
        pass

    def validate_adb_connection(self):
        # adb devices 확인
        pass

    def validate_test_products(self):
        # JSON 구조 검증
        # 상품 ID, 키워드 누락 체크
        pass

    def validate_device_status(self):
        # 배터리 20% 이상
        # 온도 45도 이하
        pass

    def create_directories(self):
        # data/rankings, data/results, logs 생성
        pass

중요도: ★★★☆☆
```

---

### 7. 모니터링 및 대시보드 ⚠️ **선택**
```
파일: src/utils/monitor.py
상태: 미구현 ❌

필요 기능:
- [ ] 실시간 진행 상황 표시
- [ ] 현재 실행 중인 상품/케이스 표시
- [ ] 누적 통계 (평균 순위 변동)
- [ ] 예상 완료 시간 계산
- [ ] 에러 발생 알림

중요도: ★★☆☆☆ (Nice to have)
```

---

### 8. 좌표 매핑 파일 ⚠️ **중요**
```
파일: config/screen_coordinates.json
상태: 미구현 ❌

Pure ADB 사용 시 필요:
{
  "screen_resolution": {
    "width": 1080,
    "height": 2400
  },
  "coordinates": {
    "naver_search_box": {"x": 540, "y": 200},
    "shopping_tab": {"x": 400, "y": 300},
    "cart_button": {"x": 540, "y": 1800},
    "review_section": {"x": 540, "y": 1500}
  }
}

중요도: ★★★☆☆ (Appium 사용 시 불필요)
```

---

## 🔥 우선순위 요약

### P0 (최우선 - 프로젝트 진행 불가)
1. **브라우저 자동화 모듈** (`src/automation/browser.py`)
2. **시나리오 실행 모듈** (`src/automation/scenario_executor.py`)
3. **메인 실행 스크립트** (`main.py`)

### P1 (중요 - 빠른 시일 내 필요)
4. **tracker.py 업데이트** (executed_case 필드)
5. **test_products.json 구조 변경** (그룹 기반)
6. **case_comparison.py 업데이트** (그룹 분석)
7. **에러 핸들러** (`src/utils/error_handler.py`)

### P2 (필요 - 안정성 향상)
8. **환경 검증** (`src/utils/validator.py`)

### P3 (선택 - 편의성)
9. **모니터링 대시보드** (`src/utils/monitor.py`)
10. **좌표 매핑 파일** (Pure ADB 사용 시)

---

## 🛠️ 다음 단계 제안

### 옵션 1: 프로토타입 빠른 검증
```
1. browser.py 간단 버전 구현 (Pure ADB)
2. 단일 케이스만 실행하는 simple_main.py
3. 1개 상품으로 10회 테스트
→ 순위 변동 확인 후 본격 구현
```

### 옵션 2: 완전 구현
```
1. browser.py 완전 구현 (Appium)
2. scenario_executor.py 5개 케이스 구현
3. main.py 100회 반복 프레임워크
4. 에러 핸들링 추가
5. 10개 상품 전체 테스트
```

### 권장: 옵션 1 → 검증 → 옵션 2
1주차: 프로토타입 → 순위 반영 확인
2주차: 완전 구현 → 전체 테스트

---

## 📋 체크리스트 (빵꾸 확인)

### 데이터 흐름
- [x] 테스트 상품 정의 → test_products.json
- [ ] 순위 체크 → checker.py ✅
- [ ] 시나리오 실행 → ❌ **브라우저 자동화 없음**
- [ ] IP 변경 → mobile.py ✅
- [ ] 순위 추적 → tracker.py ⚠️ (executed_case 미저장)
- [ ] 분석 → case_comparison.py ⚠️ (그룹 분석 미구현)

### 실행 가능 여부
- [ ] 1회 테스트 실행 가능? ❌ **불가능 (브라우저 자동화 없음)**
- [ ] 100회 반복 실행 가능? ❌ **불가능 (main.py 없음)**
- [ ] 에러 시 복구 가능? ❌ **불가능 (error_handler 없음)**
- [ ] 결과 분석 가능? ⚠️ **부분 가능 (그룹 분석 미구현)**

---

**작성일**: 2025-11-01
**상태**: 격차 분석 완료
**다음 작업**: 브라우저 자동화 모듈 구현 시작
